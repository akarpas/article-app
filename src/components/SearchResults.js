import React from 'react';
// import PropTypes from 'prop-types';
import style from './SearchResults.scss';

const SearchResults = () => {
    return (
        <div className={style.resultsContainer}>Results</div>
    );
};

export default SearchResults;

SearchResults.propTypes = {};
